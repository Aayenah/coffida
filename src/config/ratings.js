// ? define what 'high' means for ratings

const MIN_OVERALL = 3; // min overall rating to show in 'Top Rated' carousel
const MIN_QUALITY = 3; // min quality rating to show in 'High Quality' carousel
const MIN_PRICE = 3; // min price rating to show in 'Best Prices' carousel
const MIN_DISTANCE = 1000; // min distance to show in 'Nearby Cafes' carousel

module.exports = { MIN_OVERALL, MIN_QUALITY, MIN_PRICE, MIN_DISTANCE };
