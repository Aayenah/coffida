// ? define what 'high' means for ratings

const MIN_OVERALL = 4; // min overall rating to show in 'Top Rated' carousel
const MIN_QUALITY = 2; // min quality rating to show in 'High Quality' carousel
const MIN_PRICE = 4; // min price rating to show in 'Best Prices' carousel
const MIN_DISTANCE = 2; // min distance to show in 'Nearby Cafes' carousel

module.exports = { MIN_OVERALL, MIN_QUALITY, MIN_PRICE, MIN_DISTANCE };
